-- ============================================================
-- 0002_drop_is_seed.sql
-- entries.is_seed カラムを削除
-- 理由: UI から削除済み（2026-03-22）。シード機能は廃止。
-- ============================================================

alter table entries drop column if exists is_seed;
