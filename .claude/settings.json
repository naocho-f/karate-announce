{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "command -v jq >/dev/null 2>&1 || exit 0; f=$(jq -r '.tool_input.file_path // \"\"'); echo \"$f\" | grep -qE '\\.(ts|tsx)$' && cd /Users/naoki/RubymineProjects/karate-announce && npx tsc --noEmit 2>&1 || true",
            "statusMessage": "型チェック中..."
          }
        ]
      }
    ]
  }
}
