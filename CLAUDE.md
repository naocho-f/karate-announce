# karate-announce 開発ルール

> **このファイルが唯一のルール源（Single Source of Truth）**
> 「覚えて」「次からやって」等の継続的指示は必ずこのファイルに追記する。
> Memory ファイルは補足情報（背景・外部リファレンス）のみ。ルール自体は置かない。
> この方針はユーザーと合意済み（2026-03-25）。理由：Memory ファイルはコンテキスト圧縮で
> 失われることがあり、CLAUDE.md だけが毎セッション確実に読み込まれるため。

---

## プロジェクト概要
- Next.js 16（App Router）+ TypeScript + Tailwind CSS 4
- DB: Supabase（PostgreSQL）
- 音声: OpenAI TTS
- デプロイ: Vercel（Stop hook 経由で自動）
- パッケージマネージャ: npm
- ブランチ戦略: **main 直接プッシュ**（feature ブランチは使わない）
- Lint / Formatter: なし（Next.js ビルトインの型チェック + `npm run build` で検証）

## ディレクトリ構成
| ディレクトリ | 役割 |
|-------------|------|
| `app/` | Next.js App Router（ページ・API ルート・レイアウト） |
| `lib/` | 共通ロジック（DB クライアント、ユーティリティ、型定義） |
| `components/` | 共通 UI コンポーネント |
| `docs/` | 機能別の詳細仕様書 |
| `__tests__/api/` | API ルートのテスト（Vitest） |
| `__tests__/unit/` | `lib/` のユニットテスト（Vitest） |
| `__tests__/e2e/` | E2E テスト（Playwright） |
| `public/` | 静的ファイル |
| `supabase/` | Supabase 関連設定 |
| `scripts/` | Git hook 等の開発支援スクリプト |

## よく使うコマンド
| コマンド | 用途 |
|---------|------|
| `npm run dev` | 開発サーバー起動 |
| `npm run build` | ビルド（型チェック含む） |
| `npx vitest run` | 全ユニット + API テスト |
| `npm run test:unit` | ユニットテストのみ |
| `npm run test:api` | API テストのみ |
| `npx playwright test` | E2E テスト（現在無効化中） |
| `npx tsc --noEmit` | 型チェックのみ |
| `vercel ls` | デプロイ状況確認 |

## 環境変数
- `.env.local` で管理。`.gitignore` 済み。
- 新しい環境変数を追加する場合は、Vercel の Environment Variables にも追加すること。
- 必要な変数: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY`, `ADMIN_PASSWORD`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `NEXT_PUBLIC_APP_MODE`

## Vercel デプロイ時の注意事項
- **レート制限**: 公開エントリーAPIのレート制限はin-memory Map実装。Vercelのサーバーレス関数はコールドスタートでメモリがリセットされるため、トラフィック集中時にレート制限が効かない場合がある。大会当日に大量申込が予想される場合は、Supabase RLS や外部レート制限サービス（Upstash等）への移行を検討すること。
- **関数タイムアウト**: Vercel Free は10秒、Pro は60秒。メール送信を含むエントリーAPIは処理時間が長くなる可能性がある。

## 計画（大きな機能の場合）
- 複数ファイルにまたがる変更や新機能追加の場合は、**先に計画を出してレビューを受けてから実装する**。
- 小さなバグ修正や1ファイルの変更は直接実装してよい。
- **計画→実装のフロー:**
  - 計画が承認されたら、**基本はメインセッションで直接実装する**（文脈を活かせて精度が高い）
  - 特に大きなタスク（多ファイル変更、長時間が予想される場合）のみ、implementer での実行を提案する：「長時間かかるおそれがあるので implementer での実行をおすすめします。どうしますか？」
  - implementer を使う場合は `/tmp/claude-plan.md` に計画を書き出し、`/tmp/claude-implementer-running` でフラグ管理。push先はプロジェクト設定を引き継ぐこと

## 実装完了フロー（TDD 駆動・この順序で必ず実行）

> **重要**: このフローは機能追加・修正・バグ修正すべてに適用する。順序をスキップしない。

**実装開始時のタスク作成（必須）**: 実装に入る前に TaskCreate で Step 1〜8 のタスクリストを作成する。タスクリストがチェックリストとして機能し、ステップの飛ばしを防ぐ。各 Step の作業開始時に in_progress、完了時に completed にする。

### Step 1: 影響範囲の把握 → 仕様書を先に更新
- **実装の前に、変更対象のファイルで `scripts/impact-check.sh` を実行し、関連する仕様書・テスト・画面を把握する。** 列挙されたファイルを読み、現状を理解してから仕様書の更新に入る。これにより「仕様書にあるのに未実装」「テストがあるべきなのにない」等の既存の不整合にも気づける。
- **実装の前に仕様書を更新する。** コード変更より先に「何を作るか」を仕様書に記載する。
- 新機能 → `docs/` に詳細仕様を追記。SPEC.md に画面/APIが増える場合は一覧にも追加。
- 既存機能の変更 → `docs/` 内の該当仕様書の記述を修正。
- バグ修正 → 仕様書の記述が正しいか確認し、必要なら仕様書も修正。
- **SPEC.md と docs/ の役割分担:**
  - `SPEC.md`: 全体概要のみ。画面一覧は名前+1行説明+docs/へのリンク。機能詳細は書かない
  - `docs/`: 各機能の詳細仕様。新機能の詳細はここに書く
- **SPEC.md 変更履歴**: 種類（新機能・変更・バグ修正）を問わず、**全ての変更で必ず1行追記する**（Step 6 で SPEC.md が diff に含まれる必要があるため）。
  - **書式**: `- **機能名（YYYY-MM-DD）**: 1行説明`（例: `- **よみがな必須の親フィールド連動（2026-04-03）**: 親が任意なら読み仮名も自動的に任意にする`）
  - **肥大化防止**: 変更履歴の行数が100行を超えたら、古い方から `docs/CHANGELOG.md` に移動して50行まで減らす。`docs/CHANGELOG.md` には日付の新しい順で追記する。

### Step 2: テストを先に書く（Red）
- **仕様書の記述に基づいてテストを追加・修正する。** 実装の前にテストを書く。
  - API ルートの追加・変更 → `__tests__/api/` に正常系テストを追加・更新
  - `lib/` の純粋ロジック追加・変更 → `__tests__/unit/` にユニットテストを追加・更新
  - UIの追加・変更・リファクタ時は関連する E2E テスト（`__tests__/e2e/`）も追加・更新する
  - **E2E 無効化中の運用**: E2E テストは**書くが実行しない**。リリース前にまとめて通す。
- **ユニット/API テストで検証困難な場合**（コンポーネント内のレンダリングロジック等）: E2E テストで代替するか、手動確認で代替する。その場合はコミットメッセージに「手動確認済み: 〇〇」と記載する。
- テストを実行し、**追加したテストが失敗することを確認する**（Red 状態）。
  - 失敗しない場合 → テストが正しく書けていないか、既に実装済み。テストを見直す。
- 既存テストが壊れた場合はテストを修正してから進める（テストを削除して通すのは禁止）。
- E2E テストが壊れた場合は、まず実装とテストのどちらが正しいか調査・判断する。意図的なUI変更ならテストを更新、意図しない変更（リグレッション）なら実装側を修正する。
- **E2Eテストは本番DBに接続して実行される。既存データを絶対に削除・変更しない。** テストデータは必ず自分で作成し、テスト終了時に自分で作成したデータのみを削除する。
- ~~コミット前に `npx playwright test` で E2E テスト全通過を確認する。~~ **現在無効化中。本番リリース前に必ず有効化して全通過を確認すること。**
- **テストファイル命名規約**:
  - ユニット / API テスト: `*.test.ts`（例: `bracket.test.ts`）
  - E2E テスト: `*.spec.ts`（例: `entry-form.spec.ts`）

### Step 3: 実装（Green）
- `npm run dev`, `curl` 等は確認不要で実行してよい。
- テストが通るように実装する。
- 以下の3つを**すべて**実行し、全パスを確認する:
  1. `npx tsc --noEmit` — 型チェック（GitHub Actions と同じチェック）
  2. `npx vitest run` — 全テスト通過（Green 状態）
  3. `npm run build` — ビルドエラーなし
- テスト・型チェック・ビルドのいずれかが失敗した場合は、この Step 3 内で修正→再実行を繰り返す。Step 4 以降には進まない。

### Step 4: チェックリスト（該当する場合のみ）

**データフロー検証（APIを変更・追加した場合）**
- フロントエンドが `fetch` で送っているフィールド名
- APIルートが `request.json()` で受け取っているフィールド名
- `insert` / `update` でDBに渡しているフィールド名
- → 3点が一致しているか確認。不一致があれば修正。

**エラーハンドリング確認（fetch を追加・変更した場合）**
- `fetch` のレスポンスを `res.ok` でチェックしているか
- APIエラー時に UI が誤った状態に更新されないか（楽観的更新は特に注意）
- エラー時にユーザーに通知しているか（alert または UI 表示）

**ローディング状態確認（ボタン・フォームを追加・変更した場合）**
- 処理中はボタンが `disabled` になっているか
- ローディング中に空のリストや「登録なし」テキストが表示されないか

**表示確認（データの保存・取得を変更した場合）**
- 保存したデータが画面に正しく反映されるか、コードを追って確認
- 画面を再ロードしたときに正しく表示されるか

**ナビゲーション確認（ページを追加・移動した場合）**
- 新規ページへのリンク/タブが既存のナビゲーションに追加されているか
- URL直アクセスでしか到達できないページが存在しないか
- 全管理画面ページがメインの管理画面（`/admin`）のタブ・サブタブからアクセス可能か確認

**状態管理の整合性確認（state・ref・イベントリスナー・永続化を追加・変更した場合）**
- 状態遷移の全パターン（on→off、off→on、初期化、リセット）で正しく動くか
- リソース取得（addEventListener, setInterval, subscribe）と解放（remove, clear, unsubscribe）が対になっているか。useEffect のクリーンアップ、finally、条件分岐の全経路を確認
- 永続データ（IndexedDB, localStorage）と揮発データ（メモリ変数, ref）の整合性: リロード後に揮発データが失われても永続データと矛盾しないか

**エクスポート関数の全呼び出し確認（lib/ にモジュールを追加・変更した場合）**
- 新しく export した関数が、**使われるべき全画面で実際に import・呼び出しされているか**を grep で確認
- 特に「基盤モジュールのコミット」と「画面統合のコミット」を分けた場合、統合コミットで漏れた画面がないか全画面をチェック
- export したが呼び出し箇所がゼロの関数（dead export）が残っていないか確認。テストのみで使われている関数は本来アプリコードでも使われるべきか判断する
- 同じ機能を持つ画面（例: `/court/[court]` と `/court`（統合コート画面））で片方だけ統合して片方を漏らしていないか

**マルチテナント化への配慮（DBテーブル・設定・認証を変更した場合）**
- 団体固有のハードコード値（テキスト・ルール等）がないか → DB or config に外出しする
- 新しい外部サービス連携は環境変数で切り替え可能か
- Supabase 固有機能に依存していないか（PostgreSQL 標準の範囲で書く）
- 将来 `tenant_id` で分離できる構造になっているか

**DB/RPC 整合性確認（supabase.rpc() を使用する場合）** ※ pre-commit hook で自動チェック済み
- コミット時に hook が自動検出するが、**RPC を追加した時点で**マイグレーションファイルも同時に作成すること（コミット時に初めて気づくのでは遅い）

### Step 5: セルフレビュー（繰り返し）

**5a. `scripts/impact-check.sh` を実行する（必須）。** 変更ファイルを入力し、列挙された関連ファイルを**全て**読む。変更が関連ファイルと矛盾していないか、関連テストが変更を反映しているか確認する。列挙されたファイルを読まずに「問題ない」と判断してはならない。

**5b.** 以下の4点が完全に一致するまで修正を繰り返す。問題がなくなるまでコミットしない。
1. **仕様書の記述**（docs/ + SPEC.md）
2. **テストケース**（テストの期待値が仕様に沿っているか、仕様に記載した全項目にテストがあるか）
3. **実装コード**（実装が仕様通りか、テストが全通過するか）
4. **UI の見た目確認**（CSS・レイアウト・テキスト変更を含む場合）
  - `npm run dev` で開発サーバーを起動し、変更した画面を実際にブラウザで確認する
  - 要素の重なり・はみ出し・余白の崩れがないか目視チェック
  - コードの diff だけで「問題ない」と判断しない

不整合を見つけたら、仕様書・テスト・実装のどれを修正すべきか判断し、修正してから再確認する。

**追加チェック（lib/ にモジュールを追加した場合）**:
- 今回 export した関数を grep し、**テストのみで使われていてアプリコードで未使用のもの**がないか確認する。
  テストのみ = 統合漏れの可能性が高い。

### Step 6: コミット
- **1つの実装に対して1回。分割コミット禁止。**
- `git status` で意図しないファイル（デバッグ用一時ファイル等）が含まれていないか確認してから `git add -A && git commit` を1回だけ実行する。
- SPEC.md が diff に含まれていないコミットは禁止。SPEC.md のヘッダー「最終更新」日付も当日に更新すること。
- コミットメッセージは実装内容を日本語で簡潔に（例：「対戦表作成後に該当トーナメントへスクロール」）。
- 未コミットの変更が残っていると Stop hook がブロックする。必ず commit まで完了させてから応答を終えること。

### Step 7: プッシュ・デプロイ
- コミット後に `git push origin main` を実行する。（Vercel の GitHub 連携が自動デプロイする）
- `vercel --prod` は実行しない。

### Step 8: デプロイ完了確認
- 完了報告の前に `vercel ls` でデプロイが `● Ready` になっていることを確認する。
- Ready になっていなければ30秒待ってから確認、以降10秒ごとにリトライして Ready を待つ。
- **デプロイ完了を確認してから報告する。**

## DBスキーマ変更
- DBカラム追加・テーブル作成等が必要な場合、**ユーザーに依頼せず自分で** Supabase Management API 経由で実行する。確認も不要。
- 実行方法は memory/reference_supabase_sql.md を参照。
- 既存データのマイグレーションも同時に実行する。
- **マイグレーションファイル必須**: SQL を Supabase に実行したら、同じ内容を `supabase/migrations/` にも `CREATE OR REPLACE` 形式で保存する。RPC 関数・テーブル変更・インデックス等すべて対象。マイグレーションファイルがないと新環境構築時に再現できない。

## コミュニケーション
- 日本語は丁寧語（「〜しました」「〜です」）を使うこと。タメ口・体言止め禁止。
- 確認は最小限にする。このファイルに手順が書いてあることは確認不要で実行する。

## 判断ルール
- **機能の削除・無効化・方針変更を勝手に判断しない。** 必ずユーザーに確認してから行う。
- 「〜が難しいので諦めます」ではなく「〜が難しいですが、A案/B案どちらにしますか？」と選択肢を提示する。
- 判断に迷うことは全てユーザーに聞く。自分の判断で進めない。
- **最もシンプルな解決策を最初に選ぶ。** ベストプラクティスが明確なものは迷わずそれを選ぶ。回りくどいワークアラウンドを検討する前に、正攻法で解決できないか考える。

## ミス防止
- 同じミスを2回繰り返したら、3回目が起きないよう仕組み（Hook・スクリプト・設定）で対処する。
- 「次から気をつけます」だけで終わらせない。対策後は何をどう変えたか報告する。
- **自分で宣言したことは必ず実行する。** 「今後は〜します」と言ったらそれはルールになる。守れないなら言わない。
- **同じ修正を2回試しても改善しない場合**: 立ち止まって根本原因を分析する。3回目の同じアプローチは禁止。代わりに以下を行う:
  1. 問題を再定義する（本当の原因は何か？）
  2. 関連コードを広く読み直す（狭い範囲だけ見ていないか？）
  3. 異なるアプローチを検討してユーザーに提示する
- **フィールド追加・変更時は影響範囲を網羅的に確認する。** DB（カラム）→ 型定義 → API（受け取り・保存）→ UI（入力・表示）→ ロジック（フィルタ・計算）→ テスト、の全レイヤーを漏れなく修正する。1箇所でも漏れるとデータが保存されない・表示されない不具合になる。
- **あらゆる追記・変更時の確認**: CLAUDE.md・settings.json・Memory 等、どのファイルへの追記・変更でも、既存内容との重複・矛盾・競合がないか毎回確認すること。

## Pre-commit Hook（自動整合性チェック）
- ソース: `scripts/pre-commit`（Git 管理）→ `.git/hooks/pre-commit` にコピーして使用
- **チェック内容**:
  1. SPEC.md がコミットに含まれているか
  2. SPEC.md ヘッダーの「最終更新」日付が当日か
  3. コード内の全 `rpc()` 呼び出しに対応するマイグレーションファイルが存在するか
  4. TypeScript 型チェック（`tsc --noEmit`）
- hook を更新したら `scripts/pre-commit` も同期すること。新環境では `cp scripts/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit` で設定

## UIスタイルガイド（随時追記）
- **タブのレイアウト**: サブタブは `grid grid-cols-{タブ数}` で横幅を均等割にする。`flex` で左寄せにしない。

## 確認ダイアログ回避ルール（全コマンド共通）
- `$()` コマンド置換を使わない（値を事前計算して埋め込む）
- `~` チルダ展開を使わない（フルパスで書く）
- `\;` 等のバックスラッシュ+シェル演算子を使わない（xargs や専用ツールで代替）
- パイプ+複数行スクリプトを使わない（コマンドを分けて実行）
- `.claude/` 内にファイルを書き込まない（一時ファイルは `/tmp/` を使う）

## 不具合報告の対応フロー
- ユーザーから不具合報告の修正を依頼されたら（「不具合修正お願い」「バグ直して」等）:
  1. `GET /api/bug-reports` で未対応（status: open）の報告を取得
  2. **一覧と方針を提示して承認を得る**:
     - 報告の一覧（件数・各報告の要約）を提示する
     - 各報告について原因の見立てと修正方針を簡潔に説明する
     - 再現できない・内容が不明な報告は「確認が必要」と明示する
     - 複数件ある場合は優先順の提案も含める
     - **ユーザーの承認を得てから修正に着手する**
  3. 承認後、`PATCH /api/bug-reports/{id}` で `{"status":"in_progress"}` に変更し、**TaskCreate で Step 1〜8 のタスクリストを作成してから** TDD 駆動で修正を実施（実装完了フローに従う）
  4. **1件修正するごとに**コミット→ `PATCH /api/bug-reports/{id}` でステータス（resolved）・対応内容・修正バージョンを更新する。関連する複数報告をまとめて1コミットで修正してもよい
  5. 全件完了後、管理画面（設定 → 不具合報告）で確認できる状態にしてから、対応結果をまとめて報告する
- API呼び出し方法: admin_auth クッキーの値は `/tmp/admin-auth-cookie` から読み取る。ファイルが存在しない場合（再起動後等）はユーザーに確認する。
  ```
  # 取得（結果をファイルに保存してから処理）
  curl -s "https://karate.naocho.net/api/bug-reports" -H "Cookie: admin_auth=<COOKIE値>" -o /tmp/bug-reports.json
  # → 別コマンドで python3 -c "..." で /tmp/bug-reports.json を読む

  # 更新
  curl -s "https://karate.naocho.net/api/bug-reports/{id}" -X PATCH -H "Cookie: admin_auth=<COOKIE値>" -H "Content-Type: application/json" -d '{"status":"resolved","resolution":"対応内容","fixed_in_version":"コミットSHA先頭7桁"}'
  ```

## 全体レビュー
- 「全体レビュー」「全体レビューして」と指示されたら、以下の全観点でレビューを実行する:
  1. **docs/ 仕様書と実装の一致**: docs/ 内の各仕様書に記載された仕様が、実際のコードと一致しているか
  2. **仕様の矛盾・一貫性**: 仕様書内で矛盾した記述や一貫性のない定義がないか
  3. **仕様書間の整合性**: SPEC.md と docs/ 内の個別仕様書の間で齟齬がないか
  4. **テストカバレッジ**: 仕様に記載されているがテストが実装されていない箇所はないか（ユニットテスト・APIテスト・E2Eテスト全て）
  5. **テスト期待結果と仕様の紐づき**: テストの期待値が仕様書の記述と整合しているか
  6. **DB/RPC 整合性**: コード内の `supabase.rpc()` 呼び出しに対応するマイグレーションファイルが全て存在するか
  7. **Dead code・孤立ルート**: アプリコードから一切呼ばれていない API ルートや、テストのみで使用される export がないか
  8. **SPEC.md ヘッダー日付**: 「最終更新」日付が実際の最終変更日と一致しているか
  9. **その他必要な観点**: セキュリティ、エッジケース、型安全性、パフォーマンス等
- 複数の Explore エージェントを並列で使い効率的に調査する。

## 継続ルールの蓄積
- 「次からやって」「覚えといて」など継続的な指示は**このファイルに追記する**（Memory ファイルではなくここ）。
- 継続的に行った方がいいと思うルールがあれば、**ユーザーに提案して確認を得てから追記する**。勝手に追記しない。
